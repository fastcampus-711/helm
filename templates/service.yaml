apiVersion: v1
kind: Service
metadata:
  name: {{ template "springboot-docker.fullname" . }}
  labels:
    app: {{ template "springboot-docker.name" . }}
    chart: {{ template "springboot-docker.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "springboot-docker.name" . }}
    release: {{ .Release.Name }}